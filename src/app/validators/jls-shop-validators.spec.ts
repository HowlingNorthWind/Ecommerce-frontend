import { JLsShopValidators } from './jls-shop-validators';

describe('JLsShopValidators', () => {
  it('should create an instance', () => {
    expect(new JLsShopValidators()).toBeTruthy();
  });
});
